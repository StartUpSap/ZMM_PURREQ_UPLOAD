projection;
strict ( 2 );
use draft;

define behavior for ZMM_C_SOLPED_CATVAL_CTA alias CatValCta
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}